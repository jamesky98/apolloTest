// Views schema
model doc_latest {
  id                     Int       @id
  doc_id                 String
  doc_level              Int
  doc_type               Int
  name                   String
  ver                    String?
  release_date           DateTime?
  expiration_date        DateTime?
  parent_id              String?
  upload                 String?
  editable_upload        String?
  comment                String?
}

model gcp_latest {
  gcp_id                     Int       @id
  enable                     Boolean
  type_code                  Int
  cal_type_id                Int
  project_id                 Int
  method                     String
  date                       DateTime?
  person                     String
  status                     String
  need_contact               Boolean
}
